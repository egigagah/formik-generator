import { Flex } from "@chakra-ui/react";
import DataTable from "@components/data-table";

export default function App() {
    return (
        <Flex flex={1} w="full">
            {/* <DataTable columns={[]} from="/test-dong" searchTerm="" /> */}
        </Flex>
    );
}
